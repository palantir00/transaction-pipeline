# ---- streaming layer (producer + consumer) ----
kafka-python==3.0.11

# ---- database ----
psycopg2-binary==2.9.12

# ---- configuration from .env ----
python-dotenv==1.2.3

# ---- tests ----
pytest==9.1.1

# NOTE: apache-airflow is deliberately absent.
# Airflow comes from a prebuilt Docker image (see docker-compose.yml).
# Installing it via pip pulls in ~100 dependencies and tends to
# conflict with everything else in this file.
