[pytest]
# Makes "from producer.producer import ..." work without installing the project
pythonpath = .
testpaths = tests
markers =
    integration: test that needs the Docker stack running (deselect with -m "not integration")
